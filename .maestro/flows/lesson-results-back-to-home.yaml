appId: ac.tutoria.mobile
name: "Results Screen Back to Home"
---
# This test starts at the results screen state
# by loading the app after a completed session
# Note: In real E2E, this would follow a full lesson completion
# For now we test the empty state redirect behavior
- launchApp
# App should land on home after loading
- assertVisible:
    text: "Good"
    timeout: 5000
